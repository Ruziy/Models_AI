Первое и основное СУЩЕСТВУЕТ 2 ТИПА (знакомых мне на этапе не дошедшего до нейросетей) алгоритмов РЕГРЕССИ и КЛАССИФИКАЦИЯ. 
Регрессия это линейная и тд..
Классификация это логистическая (хоть и регрессия но не тот случай), catboost, нейронки.
При работе с skclearn ВСЕ модели внутри имеют НОРМАЛИЗАЦИЮ, она же from sklearn.preprocessing import StandardScaler.
Использовать нормализация для CatBoost НЕ НАДО.


============Нейронки
Основной сет данных keras+tensorflow
Однослойный перцептрон представляет собой  логистич. классификатор.
Двуслойный перцептрон у которого два слоя первый слой сверточный, а второй классификаторный == числу классов.
